package org.cquemin.prioritarr

import kotlin.test.Test
import kotlin.test.assertTrue

/**
 * Smoke test that proves the build pipeline is wired up — compiles,
 * runs under JUnit 5, and produces a test report. Real tests arrive
 * with Task 2.
 */
class ScaffoldTest {
    @Test
    fun buildPipelineIsAlive() {
        assertTrue(true)
    }
}
